#!/usr/bin/env bash
set -euo pipefail

test "$#" -eq 4 && test "$1" = "-exclude-generated" && test "$2" = "-severity" && test "$3" = "high" && test "$4" = "./..."
printf '%s\n' 'controlled known-bad high-severity Go security probe'
exit 1
